export interface CreteateSaleDetails {
    saleId: string;
    productId:  string;
    quantity: number
    price : number   
    subtotal: number  
}

export interface UpdateSaleDetails {
    saleId: string;
    productId:  string;
    quantity: number
    price : number   
    subtotal: number  
}